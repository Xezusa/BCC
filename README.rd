# BUILD CONFIG COMPILE

cmake and make are bloat
Compiler is all you need.
Once compiled, run it once and you're done.

Whichever platform you are building for, just put all build functions into one build_chain()

gcc -o bcc.exe bc.c
.\bcc.exe
